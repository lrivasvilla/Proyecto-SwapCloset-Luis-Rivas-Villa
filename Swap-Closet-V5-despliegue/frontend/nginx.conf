server {
  listen 80;
  server_name _;

  root /usr/share/nginx/html;
  index index.html;

  # Angular/Ionic SPA routing (por tus rutas /home /explorar /perfil-otro/:id, etc.)
  location / {
    try_files $uri $uri/ /index.html;
  }
}
